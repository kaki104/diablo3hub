<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Diablo3Hub.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:viewModels="using:Diablo3Hub.ViewModels"
    x:Class="Diablo3Hub.Views.HeroPage"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:HeroPageViewModel/>
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveVisualStateGroup">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!--  header  -->
        <controls:PageHeader x:Name="pageHeader" Frame="{x:Bind Frame}"
                             RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True"/>

        <Pivot RelativePanel.Below="pageHeader"
               RelativePanel.AlignBottomWithPanel="True"
               RelativePanel.AlignLeftWithPanel="True"
               RelativePanel.AlignRightWithPanel="True">
            <PivotItem Header="Stat">
                <Canvas>
                    <Canvas.Background>
                        <ImageBrush Stretch="Uniform" ImageSource="ms-appx:///Assets/Backgrounds/demon-hunter-female-item.png"/>
                    </Canvas.Background>
                    <Rectangle Width="74" Height="149" Fill="#FF3A220A" Stroke="#FF110A05" Canvas.Left="6" Canvas.Top="354"/>
                    <Image Source="ms-appx:///Assets/p4_unique_sword_1h_01_demonhunter_male.png" 
                           Canvas.Left="10" Canvas.Top="365"/>
                </Canvas>
            </PivotItem>
            <PivotItem Header="Item">

            </PivotItem>
            <PivotItem Header="Skill">

            </PivotItem>
            <PivotItem Header="Follower">

            </PivotItem>

        </Pivot>
    </RelativePanel>
</Page>
